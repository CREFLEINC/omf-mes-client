# POP 공통 밀도·선택 팝업 적용 기록

- 기준 설계: `omf-mes` 고정 커밋 `5b3d77376534694fb5b2d1e384bc35745d91b270`
- 기준 조항: `design/wiki/decisions-policy/공유계약.md` E-1·E-3·G-34
- 점검 화면: `apps/web/src/routes/pop.tsx`의 공개 라우트 21개

## 공통 적용

- 모든 공개 화면 루트는 `.pop-ui`를 사용한다. `lg`·`xl`·`2xl` 컨트롤 높이는 이 겹에서
  관리웹 기본 `md` 토큰으로 수렴한다. 폐기된 56·64·72px 하한은 사용하지 않는다.
- 선택형 입력은 `PopSelect`로 통일한다. 닫힌 상태에 현재 값과 `[선택]`을 분리해 보이고,
  팝업 안에서 이미 받은 후보만 로컬 검색한다. 목록 내부 스크롤, 검색어 지우기,
  `[페이지 위]`·`[페이지 아래]`, 현재 페이지와 전체 건수를 제공한다.
- 공통 헤더에는 `PopScreenNav`를 둔다. 라우트 표에 있는 활성 태스크 화면만 후보로 사용하고,
  선택 즉시 이동한다. 검색 중 API를 호출하거나 저장되지 않은 입력 확인을 추가하지 않는다.
- 기존 사번 해제 동작은 문구를 `[사용자 전환]`으로 맞췄다. 현재 활성 사번만 지우고
  `P-CO-01`로 이동하며 저장 기록·오프라인 큐는 수정하지 않는다.

## 공개 라우트 점검 결과

| 적용 결과                                  | 화면                                                                                                                                                     |
| ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 밀도 + 선택 팝업 + 공통 이동               | `P-01-01`, `P-01-02`, `P-02-05`, `P-02-06`, `P-02-07`, `P-02-08`, `P-02-09`, `P-02-11`, `P-02-13`, `P-04-01`, `P-04-02`, `P-04-03`, `P-04-04`, `P-05-02` |
| 밀도 + 공통 이동(화면 자체 선택 입력 없음) | `P-02-01`, `P-02-03`, `P-02-04`, `P-02-10`, `P-02-12`, `P-05-01`                                                                                         |
| 진입 예외                                  | `P-CO-01`: 밀도와 공통 이동은 적용하고, 이미 진입 화면이므로 사용자 전환 버튼만 숨김                                                                     |

## 경계

- API 클라이언트, 요청 본문, 호출 순서, 쿼리 키는 변경하지 않는다.
- 서버 페이지네이션 후보는 화면이 이미 받은 범위만 `PopSelect`에 전달되므로 그 범위만
  검색한다. 추가 페이지를 검색하기 위한 API 호출은 만들지 않는다.
- 화면 목록은 현재 셸에 공개된 활성 라우트가 접근 가능 후보의 정본이다. 역할별 후보가
  셸에서 별도로 공급되면 `POP_SCREEN_ROUTES`의 공급원만 교체하고 팝업 동작은 유지한다.

## 회귀 게이트

- `pop-common-rules.test.ts`: 라우트 전수, `.pop-ui`, DS `Select` 직접 사용 금지
- `pop-touch-sizes.test.ts`: 관리웹 기본 밀도와 폐기 치수 재도입 방지
- `pop-select.test.tsx`: 로컬 검색·지우기·페이지 이동·선택
- `pop-screen-nav.test.tsx`: 현재 화면 표시와 즉시 이동
