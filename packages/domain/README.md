# @omf-mes/domain

프레임워크와 무관한 업무 개념 · 업무 상태와 상태 전이 규칙 · 업무적으로 의미 있는 값과 검증 규칙(불변조건).

**허용 의존: 없음** — 프로젝트 내부의 다른 패키지에 의존하지 않는다.

OpenAPI 생성 타입(DTO)은 여기 두지 않는다 — `@omf-mes/api-client` 소유. 도메인 모델 분리는 업무 의미와 변경 영향도를 기준으로 판단하며, 단순 조회·CRUD 데이터는 DTO를 직접 써도 된다.
