/**
 * POP 셸 문구. 화면 하나가 아니라 **POP 프로그램 전체가 쓰는 자리**다 —
 * 어느 POP 화면에서나 같은 뜻으로 읽혀야 하는 것만 둔다.
 *
 * 화면별 문구는 각자의 파일에 둔다. 여기에 모으면 화면이 늘 때마다 같은 자리에서 만난다.
 */
export const popShell = {
  /** 상단 바 왼쪽 — 관리웹과 같은 코드에서 나오므로 어느 프로그램인지 밝힌다 */
  brand: 'OMF-MES POP',
  mainLabel: '본문',
  skipLink: '본문으로 건너뛰기',
} as const;
