# omf-mes-client

OMF-MES 사용자 프로그램의 클라이언트 모노레포. **React + TypeScript 코드 1벌**에서 셸 3종을 빌드한다 — 브라우저(관리웹) · Electron(POP, 산업용 패널 PC) · Capacitor(모바일, Android).

셸이 셋이어도 코드베이스는 하나다. 도메인 타입·검증 규칙·API 클라이언트·디자인 토큰·i18n 리소스·outbox는 **공유물**이고, 폼팩터 분기는 진입점·레이아웃 레벨에서만 한다.

---

## ⚠ 이 저장소는 공개(public)다

설계·기획 저장소와 서버 저장소는 비공개지만 **여기는 공개**다. 올라간 것은 되돌릴 수 없다 — 나중에 비공개로 전환해도 인덱싱된 사본과 포크는 회수되지 않는다. 커밋 전에 아래 경계를 확인한다.

### 올려도 되는 것

- **MES 일반 업무 용어** — 자재LOT · W/O · PQC · IQC · 특채 승인 · Lot Status 등. 업계 표준 어휘라 공개돼도 무방하다
- **내부 식별자** — 화면 ID(`W-01-01`·`P-02-04`), 라우트, 컴포넌트명
- **i18n 문구** — 한/베 화면 라벨·검증 메시지
- **합성 테스트 데이터** — 직접 지어낸 값

### 올리면 안 되는 것

| 금지 | 예 |
| --- | --- |
| **고객사 식별 정보** | 회사명 · 공장/사업장 위치 · 부서·담당자 실명 · 조직도 |
| **계약·견적 정보** | 금액 · 수량 · 단가 · 납기 · 제안 내역 |
| **실 운영 값** | 실제 사번 · 품목코드 · LOT 번호 · 거래처 코드 → **합성값을 쓴다** |
| **인프라 정보** | IP · 호스트명 · 포트 · 계정 · 인증서 · 접속 문자열 |
| **외부 시스템 스키마** | 고객사 ERP 테이블·컬럼명 (ERP 연계는 서버 소관이며 클라이언트에 들어올 이유가 없다) |
| **비공개 문서 내용** | 설계·기획 문서의 본문 복사·발췌·요약 |
| **이미지** | 와이어프레임 · 화면 캡처 · 워크플로우 도식 |

### 이 규칙은 코드에만 적용되는 것이 아니다

**커밋 메시지 · PR 제목/본문 · 이슈 · 코드 주석 · 테스트 픽스처 · 설정 템플릿** 전부 같은 기준이다. 공개 저장소에서는 히스토리와 메타데이터도 함께 공개된다.

비공개 저장소를 참조할 때는 **내용을 옮기지 말고 번호만 적는다**:

```
✅ 설계 근거: omf-mes#42
❌ 설계 근거: "○○공장 3라인의 특채 승인은 품질팀장이 …" (본문 복사)
```

### 실수했을 때

의심스러우면 **올리지 말고 먼저 묻는다.** 이미 올렸다면 **즉시 알린다** — history를 다시 쓰거나 force push로 조용히 덮지 않는다. 공개 저장소에서는 push된 순간 노출된 것으로 보고 대응해야 하며, 조용한 삭제는 대응 시점만 늦춘다.

---

## 하네스: 클라이언트 개발 4팀 운영

이 저장소는 「Multi Development Agent Coding Teams 운영 규칙」(정본은 저장소 밖)의 **클라이언트 개발 4팀**(`Agent : T4`)이다. 설계팀 저장소(`CREFLEINC/omf-mes`, 비공개)는 **참고만** 한다 — 업무 배정·검토 요청 처리·설계 정합성 판정은 설계팀 소관이며 이 저장소가 대신하지 않는다.

**트리거:** 이슈 착수 · 코드 작성/변경/삭제 · 테스트 판정에는 `docs/client-dev-workflow/`의 절차로 시작한다. 코드 변경이 없는 질문형 요청은 직접 응답한다.

**절대 경계:** 다른 개발팀(T1~T3)과 직접 소통하지 않는다 — 필요한 소통은 전부 설계팀과 한다 · 배정되지 않은 이슈에 손대지 않는다 · 설계 저장소 자료의 본문·와이어프레임을 이 저장소로 옮기지 않는다(위 공개 경계와 동일 원칙).

**상세 절차:** `docs/client-dev-workflow/` 참조 — 규칙의 정본이다. 이 규칙을 어떤 도구(Skill·Agent·수동 진행)로 지킬지는 각자 자유이며, `.claude/`는 개인 설정이라 이 저장소에 커밋되지 않는다.

---

## 브랜치

`main` 직접 push는 룰셋으로 차단돼 있다(PR 필수 · 브랜치 삭제 금지 · force push 금지). 작업은 브랜치에서 하고 PR로 병합한다. **룰셋의 보호 대상은 `main` 하나이며 작업 브랜치에는 적용되지 않는다.**

## 디자인 시스템

UI는 **`@crefle/web-ui`** 를 쓴다. 컴포넌트를 새로 만들기 전에 **인벤토리를 먼저 본다** — DS 저장소의 `docs/COMPONENTS.md`가 정본이고, 없는 것을 요청하는 절차는 `docs/component-requests.md`에 있다.

갭은 네 갈래로 나눈다 — **a** 그대로 있음 / **b** 컴포넌트는 있으나 변형·prop 부족(DS 이슈) / **c** 기존 컴포넌트의 **조합**(여기서 소유, 이슈 없음) / **d** 진짜 새 원시 요소(여기서 먼저 만들고 **세 번째 사용처**에서 DS로 승격 — 그전까지는 `ds-candidates/`).

**c가 가장 많다.** 조합물을 DS로 올리려는 것이 디자인 시스템이 망가지는 가장 흔한 경로다.

> DS 저장소도 공개다. **이슈에 제품 화면명·고객사명·와이어 캡처를 붙이지 않는다** — 갭은 제품의 언어가 아니라 컴포넌트의 언어로 쓴다("설비 상세의 가동률 추이"가 아니라 "시계열 차트 위 기간 선택기").

## 설계 정본은 여기 있지 않다

화면 목록·IA·프로세스·요구사항·설계 결정의 정본은 **비공개 설계 저장소**에 있다. 이 저장소는 그 결정을 구현하는 곳이며, 설계를 바꿔야 하면 여기서 정하지 말고 설계 저장소에 이슈로 올린다.

화면 착수는 설계가 이 저장소에 올리는 `[uiux→client] 착수 가능` 이슈로 시작한다 — 착수 신호·변경 통지 ⛔/⚠·질문 세 채널과 공개 저장소 경계는 **`docs/uiux-handoff.md`** 가 정본이다.
