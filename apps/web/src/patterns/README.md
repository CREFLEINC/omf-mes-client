# patterns

리소스 패턴 3종(마스터 형 · 부여/회수 형 · 버전 마스터 형)의 공통 workflow — 공통 상태 전이, 조회·저장 흐름, ETag 충돌·`STATE_LOCKED` 공통 처리.

**패턴은 특정 리소스의 이름을 알지 않으며, 특정 화면을 위한 조건 분기를 포함하지 않는다.** `routing`·`warehouse` 같은 리소스 이름으로 분기해야 한다면 그 로직은 해당 화면 슬라이스 소유다. 범용 화면 프레임워크로 키우지 않는다.
