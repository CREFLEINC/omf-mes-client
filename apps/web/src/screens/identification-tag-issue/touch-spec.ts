import type { ButtonSize } from '@crefle/web-ui';

/**
 * 이 화면의 터치 규격 — **72픽셀**.
 *
 * 스펙 §7 DS 매핑이 「발행·재인쇄 — `Button` ×2(**큰 타겟**)」로 지정했고, 그 「큰 타겟」이
 * `2xl` 이다(E-3 개정 2026-08-28 로 갭 `G-5` 해소).
 *
 * ⭐ **한 자리에 둔다.** 값을 쓰는 곳마다 `"2xl"` 을 직접 적으면, 등급이 바뀔 때 한쪽만
 * 고쳐져 같은 화면에 두 크기가 선다. 다른 POP 화면들이 같은 형태로 자기 값을 갖는다.
 *
 * ⚠ **이 화면이 소유한다** — 다른 화면 슬라이스의 같은 이름 파일을 참조하지 않는다. 화면마다
 * 스펙이 지정한 등급이 다를 수 있고, 참조하면 남의 화면 스펙이 이 화면을 바꾼다.
 *
 * ⛔ **72 를 쓰는 곳은 이 두 버튼뿐이다.** 스펙이 「큰 타겟」으로 지정한 것이 그 둘이고,
 * 목록의 [ 선택 ] 같은 보조 조작까지 여기에 맞추면 화면에서 무엇이 주된 조작인지 흐려진다.
 */
export const POP_TOUCH_SIZE: ButtonSize = '2xl';
