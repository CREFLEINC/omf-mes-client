/**
 * 이 화면이 **보내는** 코드 값 — 확정된 것과 아직 아닌 것을 한 곳에서 가른다.
 *
 * 값을 화면 곳곳에 흩어 두면 확정 통지가 왔을 때 무엇을 고쳐야 하는지 셀 수 없다. 그래서
 * 보내는 코드는 전부 여기 모으고, **자리표시인 것에는 그 사실을 붙인다**(전례 `P-01-01`).
 *
 * | 값 | 상태 | 근거 |
 * | --- | :-: | --- |
 * | `RESULT_SOURCE_CODE` | ⚠ **자리표시** | 착수 이슈 #74 · 검토 요청 `omf-mes#393` |
 */

/**
 * ⚠ **자리표시다.** 계약 `ProductionResultCreate.resultSourceCode` 는 **필수**인데 값 목록과
 * 공통코드 그룹 이름이 아직 정해지지 않았다 — 계약 설명이 그 사실을 직접 적어 두었고, 스펙
 * §8 미결 6번도 「`W-06-06` 소관」까지만 말한다.
 *
 * ⛔ **그렇다고 이 칸을 비우고 보낼 수는 없다** — 필수라 서버가 거부한다. 이 화면은 사람이
 * 키패드로 수량을 쳐 넣는 자리이므로 원천은 언제나 **수기 입력**이고, 그 뜻의 문자열을
 * 자리표시로 박아 둔다.
 *
 * 값이 확정되면 **이 줄 하나만** 바꾼다 — 요청 조립·화면에는 건드릴 것이 없다.
 * 확정 경로: `GET /mdm/code-values?codeGroupCode=…`(그룹 이름도 함께 확정된다).
 */
export const RESULT_SOURCE_CODE = 'MANUAL';
