import type { ButtonSize } from '@crefle/web-ui';

/**
 * 이 게이트의 터치 규격 — **72픽셀**(스펙 §4 액션바 88 · §7 「G-5 해소 · `2xl`」).
 *
 * ⭐ **위험 등급이다.** [ 우회하고 시작 ] 은 통제를 건너뛰고 기록을 남기는 조작이라
 * 되돌릴 수 없다.
 *
 * ⭐ **POP 이 자기 값을 갖는다** — 공유 부품의 기본값(모바일 규격)에 기대지 않는다.
 * 기대면 남이 그 기본값을 바꿀 때 이 화면의 키 크기가 조용히 따라 바뀐다(공유계약 D-4).
 *
 * 이 화면이 소유한다 — 다른 화면 슬라이스의 같은 이름 파일을 참조하지 않는다.
 */
export const POP_TOUCH_SIZE: ButtonSize = '2xl';
