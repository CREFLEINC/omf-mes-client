import type { ButtonSize } from '@crefle/web-ui';

/**
 * 이 화면의 터치 규격 — **72픽셀**(스펙 §4 액션바 88 안의 버튼 · §7 「G-5 해소 · `2xl`」).
 *
 * ⭐ **POP 이 자기 값을 갖는다.** 공유 부품의 기본값(모바일 규격)에 기대지 않고 늘 명시해
 * 넘긴다 — 기대면 남이 그 기본값을 바꿀 때 이 화면의 키 크기가 **조용히 따라 바뀐다**
 * (공유계약 D-4).
 *
 * ⭐ **한 자리에 둔다.** 사번 키패드와 액션바가 각자 적으면 한쪽만 고쳐져 같은 화면에 두
 * 크기가 선다.
 *
 * 이 화면이 소유한다 — 다른 화면 슬라이스의 같은 이름 파일을 참조하지 않는다.
 */
export const POP_TOUCH_SIZE: ButtonSize = '2xl';
