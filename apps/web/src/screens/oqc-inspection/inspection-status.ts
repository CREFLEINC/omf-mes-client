/**
 * 검사 결과의 상태 코드 — **영문이다.**
 *
 * ⛔ **한국어 「확정」이 아니다.** 2026-09-02 계약 개정이 이 자리의 한국어 enum(「작성중」·
 *    「확정」)을 걷어 냈다 — 계약 7벌에서 유일한 한국어 enum 이었고, 표시명이 2개국어라 enum
 *    이면 화면이 표시명을 갖게 되기 때문이다(`InspectionResultCreate.statusCode` 주석 · K-5).
 *    서버는 코드 그룹 `INSPECTION_RESULT_STATUS`(값 `DRAFT`·`CONFIRMED`)로 검증한다.
 *
 * ⛔ **이 화면은 그 개정을 놓쳤었다.** 저장이 「확정」을 보내 **400 INVALID 로 거부됐고**
 *    (`SHIP-FINAL-01` D1 · 2026-09-17 실측), 응답을 읽는 두 자리도 같은 값으로 비교해
 *    **확정된 회차를 확정으로 알아보지 못했다** — 저장이 성공했어도 폼이 안 잠기고 이력이
 *    「진행 중」으로 남았을 자리다. PQC·IQC 화면은 이미 영문을 쓰고 있었다.
 *
 * ⭐ **한 곳에만 둔다.** 종전에는 같은 문자열이 이 슬라이스 안에 셋으로 흩어져 있었고, 그래서
 *    한 곳을 고쳐도 나머지 둘이 남았다.
 */
export const CONFIRMED_STATUS = 'CONFIRMED';
