# 클라이언트 개발 하네스

이 저장소에서 코드·문서·테스트를 변경하기 전에 `pnpm workflow:check`를 실행한다. 로컬 상태가 아직 없으면 `docs/client-dev-workflow/README.md`의 초기화 절차를 먼저 따른다.

## 협업 경계

- 설계 결과물이 제품 요구사항의 정본이다. 개발팀은 코드 수준의 설계와 구현·검증 방법을 스스로 결정한다.
- 설계팀이나 다른 개발팀에 직접 연락하거나 업무를 배정하지 않는다. 설계팀이 이 저장소에 발행하는 **설계 변동 공지**만 예외로 수신한다.
- 정보 요청·설계 개선이 필요하면 요청서를 작성해 사용자에게 전달을 부탁한다. 회신을 기다리며 업무를 멈추지 않는다.
- 사용자가 전달 완료와 회신 대기를 지시한 경우에만 이 저장소에 추적 이슈를 만든다. 설계 저장소에 직접 이슈·코멘트·PR을 만들지 않는다.
- 각 팀은 처리할 일을 스스로 고르고, 이 저장소 이슈에 `Agent : T{번호}`·`Agent : Client`·`in progress`를 붙여 공개한다.

## 설계 기준 고정

- 격리된 설계 참조 클론과 그 커밋을 `.client-dev/state.json`에 고정한다. `.client-dev/`는 비공개 로컬 상태이므로 커밋하지 않는다.
- 작업 착수마다 설계 클론을 fetch/pull/reset하지 않는다. 현재 고정 커밋만 읽는다.
- 설계 변동 공지에는 공지일·배포 커밋·설계 자료 경로와 버전·달라진 지점만 있어야 한다. 상세 변경 내용은 고정 커밋으로 이동한 뒤 개발팀이 직접 분석한다.
- 공지를 수신하면 진행 중인 계획과 코드 설계를 전면 재검토한 뒤 `workflow accept-design-change`로 새 기준을 기록한다.

## 공개 저장소 경계

이 저장소는 공개다. 비공개 설계 본문·와이어프레임·화면 캡처·고객사 정보·실 운영 값·인프라 정보를 코드, 테스트, 커밋, PR, 이슈에 넣지 않는다. 공개 기록에는 화면 ID, 문서 경로, 버전, 이슈 번호 같은 포인터만 남긴다.

상세 절차와 검증 수준은 `docs/client-dev-workflow/`가 정본이다.
